// Get all elements with class name 'sub'
var elements = document.getElementsByClassName('sub');

// Loop through the elements and hide them
for (var i = 0; i < elements.length; i++) {
  elements[i].style.display = 'none';
}
